function _gradient(){

  //tablica wszystkich kolorów w gradiencie
  this.color = Array(); //Array( Array(color1,color2,color3) , Array(color1,color2,color3) )

  //tablica tytułów poszczególnych legend
  this.title = Array(); //Array( title1, title2, title3 )

  //tablica najmniejszej i największej wartości w wybranej kolumnie
  this.value = Array(); //Array( Array(min_value,max_value) , Array(min_value,max_value) )

  //podstawowe wbudowane palety kolorów
  this.palette = Array(
    Array('#7A9127','#4E6112','#BC935F','#F2E2C4','#8C4D16'),
    Array('#36333D','#C3CDDB','#92A94A','#D8D2AC','#756964'),
    Array('#2C323D','#DA1F2F','#CC0441','#3AB0A4','#FFFFFF')
  );

  //dodajemy nowy rekord do bazy
  this.add = function(){
    this.color.push( Array('') );
    this.title.push('');
    this.value.push( Array('','') );
  }

  //usuwamy rekordu z bazy
  this.remove = function( label_num ){
    this.color.splice(label_num, 1)
    this.title.splice(label_num, 1)
    this.value.splice(label_num, 1)
  }

  //fukncja akutalizująca zawarość gradientu (wyszukujemy najmniejszą i nawiększą wartość w wybranej kolumnie tabeli)
  this.generate = function( gradient_num, column ){

    //funkcja działa poprawnie w zakresie -100mln +100mln
    var min = 100000000;
    var max = -100000000;

    for(var i = 1; i < excel.parse_file.length; i++){
      if (( min > excel.parse_file[i][column] ) && ( excel.parse_file[i][column] )) { min = excel.parse_file[i][column] };
      if (( max < excel.parse_file[i][column] ) && ( excel.parse_file[i][column] )) { max = excel.parse_file[i][column] };
    }

    //przypisujemy wartość gradientu
    this.value[ gradient_num ] = Array( min , max );

  }

  //aktualizujemy wybrany kolor
  this.update_color = function(label_num,color_num,color){

  }

  //funckja wyświetlająca panel do edycji labeli
  this.show = function(){

  }

}
