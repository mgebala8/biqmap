//= require jquery
//= require_self
//= require_tree ./home

var open_login = false;
authenticity_token = <%= 2+2 %>

$(document).ready(function(){
  $('.main_logo').fadeIn(4000);

  //pokazanie menu po kliknięciu
  $('nav ul li:eq(1)').click(function(){
    if (!open_login) {
      $('.popup.login').animate({top: '200px'}, 500);
      open_login = open;
    }
  });

  $('.button').click(function(){
    alert('wysylam zapytanie');
    $.ajax({
      url: '/rest/user',
      type: 'GET',
      data: {'authenticity_token':authenticity_token},
      success: function(result) {
        //result = JSON.parse(result)
        alert(result[0])
      }
    });
  });


});
